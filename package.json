{
  "name": "blechroute-app",
  "private": true,
  "version": "0.1.0-beta.6",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --configLoader runner",
    "preview": "vite preview --configLoader runner",
    "check:public": "node scripts/check-public-readiness.mjs",
    "build:android:web": "vite build --configLoader runner",
    "android:sync": "cap sync android",
    "android:debug": "cap sync android && cd android && gradlew.bat assembleDebug",
    "testers:generate": "node scripts/generate-test-credentials.mjs",
    "testers:build-apks": "node scripts/build-test-apks.mjs"
  },
  "dependencies": {
    "@capacitor/app": "^8.0.1",
    "@vitejs/plugin-react": "^5.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/cli": "^8.5.2",
    "@capacitor/core": "^8.5.2"
  }
}
